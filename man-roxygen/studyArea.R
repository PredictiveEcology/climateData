#' @param studyArea an `sf` or `SpatVect` polygon delineating the boundaries of the study area.
#'        typically used to crop/mask rasters when `rasterToMatch` is not supplied.
#'        passed to [reproducible::postProcessTo()].
